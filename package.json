{
  "name": "test-gradient",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build  && next export",
    "deploy": "next build && next export -o docs && git checkout -- docs/.nojekyll && git add docs && git commit -m \"regen site\" && git push",
    "start": "next start"
  },
  "dependencies": {
    "bootstrap": "^5.0.2",
    "next": "10.0.7",
    "react": "17.0.1",
    "react-bootstrap": "^2.0.0-beta.4",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "@types/node": "^14.14.28",
    "@types/react": "^17.0.2",
    "autoprefixer": "^10.2.4",
    "postcss": "^8.2.6",
    "prettier": "^2.3.2",
    "tailwindcss": "^2.0.3",
    "typescript": "^4.1.5"
  }
}
